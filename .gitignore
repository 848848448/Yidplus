# ── Secrets / local config — NEVER commit these ──
.dev.vars
.dev.vars.*
.env
.env.*
*.key
*.pem
secrets.json
wrangler.toml.local

# ── Cloudflare / build ──
.wrangler/
.mf/
dist/
build/

# ── Dependencies ──
node_modules/

# ── OS / editor cruft ──
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/
*.swp
